# AI智能日历微信小程序 — 项目总览

## 交付状态：✅ 完整实现

### 完成内容

**后端（Express + SQLite）**
- ✅ JWT认证体系（微信登录 + Token管理）
- ✅ 日程CRUD完整API（创建/读取/更新/删除）
- ✅ AI建议生成服务（7大场景分类 + 专业提示词模板）
- ✅ 离线降级方案（无API Key时自动使用预设建议）
- ✅ SQLite数据库（users/schedules/suggestions三表）
- ✅ 统一API响应格式 + 错误处理

**前端（微信小程序 + WXML/WXSS/JS）**
- ✅ 4个完整页面：日历首页、添加日程、AI建议列表、个人中心
- ✅ 月视图日历（日期选择、日程标记点、月份切换）
- ✅ 日程详情弹窗（含AI建议展示、删除操作）
- ✅ 微信一键登录流程
- ✅ 统一样式系统（CSS变量、卡片、按钮、空状态等）
- ✅ 智能提示（日程示例快速填充）

**AI引擎（核心亮点）**
- ✅ 7大日程分类（出差旅行/健身运动/会议工作/学习充电/社交聚会/生活日常/健康养生）
- ✅ 关键词规则快速分类（无需额外AI调用）
- ✅ 每类专属System Prompt（时间规划 + 实用攻略 + 必备清单 + 注意事项）
- ✅ 模拟建议降级方案（离线也能正常使用）

### 关键决策

1. **分类策略**：使用关键词规则而非AI分类 — 零延迟、零成本、100%覆盖
2. **后端架构**：Express + SQLite — 轻量够用，部署简单
3. **认证方案**：JWT — 无状态，适合小程序场景
4. **AI接口**：OpenRouter兼容 — 可切换DeepSeek/Claude/GPT等多种模型
5. **降级方案**：内置模拟建议 — 无API Key也可正常使用

### 如何使用

1. 启动后端：`cd server && npm install && npm start`
2. 导入小程序到微信开发者工具
3. 配置后端URL和API Key
4. 运行测试

### 后续建议

- 接入真实微信登录（目前开发环境使用mock）
- 添加推送提醒功能（微信订阅消息）
- 接入真实AI API Key（OpenRouter）
- 添加日程分享功能
- Tab Bar图标需要放置真实PNG图片
- 部署后端到云端（Railway推荐）

## 发布上线

详细指南请参考：
- **后端部署**：`server/deploy.md`
- **小程序发布**：`PUBLISH.md`

### 发布流程概要

```
1. 后端部署 (Railway)
     ↓
配置环境变量 → 挂载存储卷 → 获取HTTPS域名 → 验证健康检查
     ↓
2. 小程序配置
     ↓
注册小程序 → 获取AppID/Secret → 配置服务器域名白名单 → 替换baseUrl
     ↓
3. 代码上传
     ↓
准备TabBar图标 → 代码检查 → 开发者工具上传 → 设为体验版
     ↓
4. 提交审核
     ↓
填写审核信息 → 配置隐私协议 → 等待审核（1-7工作日）
     ↓
5. 正式发布
     ↓
审核通过 → 发布上线 → 监控运行状态
```

### 发布前必做检查

| 检查项 | 说明 |
|--------|------|
| `app.js` baseUrl | 必须改为生产 HTTPS 域名 |
| `project.config.json` appid | 替换为真实 AppID |
| TabBar 图标 | `images/` 目录下放置6张图标文件 |
| 后端环境变量 | 配置 `NODE_ENV=production` 和 `JWT_SECRET` |
| 微信服务器域名 | 后端 HTTPS 域名已添加到白名单 |
| console.log 清理 | 小程序端无调试日志残留 |

### 推荐部署平台

- **后端**：Railway（支持 SQLite 持久化、自动 HTTPS、免费额度）
- **域名**：Railway 自动分配 `xxx.up.railway.app` 子域名
- **AI**：OpenRouter（无需自建 AI 服务，按量付费）

### 版本信息

- 当前版本：**1.0.0**
- 基础库版本：3.3.4+
- 目标平台：微信 iOS 8.0+、Android 8.0+



