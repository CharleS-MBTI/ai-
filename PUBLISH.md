# AI智能日历 - 微信小程序发布上线指南

本文档详细说明如何将 AI 智能日历小程序发布到微信正式环境。

---

## 目录

1. [前置准备](#前置准备)
2. [微信小程序注册](#微信小程序注册)
3. [服务器域名配置](#服务器域名配置)
4. [小程序代码准备](#小程序代码准备)
5. [微信开发者工具上传](#微信开发者工具上传)
6. [提交审核](#提交审核)
7. [审核注意事项](#审核注意事项)
8. [版本号与更新日志模板](#版本号与更新日志模板)
9. [发布后检查清单](#发布后检查清单)

---

## 前置准备

在开始发布之前，请确认以下事项已完成：

- [ ] 后端服务已部署到云端并获得 HTTPS 域名（参考 `server/deploy.md`）
- [ ] 后端服务健康检查通过：`https://你的域名/health` 返回 `{"status":"ok"}`
- [ ] 已安装 [微信开发者工具](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)（最新稳定版）
- [ ] 拥有一个完成实名认证的微信账号

---

## 微信小程序注册

### 步骤 1：访问小程序管理后台

打开 [微信公众平台](https://mp.weixin.qq.com/)，点击右上角 **"立即注册"**。

### 步骤 2：选择注册类型

选择 **"小程序"** 作为注册账号类型。

### 步骤 3：填写注册信息

按照页面提示填写：

| 项目 | 说明 |
|------|------|
| 邮箱 | 未被微信公众平台注册过的邮箱 |
| 密码 | 建议使用强密码 |
| 验证码 | 邮箱收到的激活验证码 |

### 步骤 4：选择主体类型

选择 **"个人"** 或 **"企业"**（推荐企业主体）：

- **个人主体**：仅需身份证信息，但功能有限制（如不能使用微信支付）
- **企业主体**：需要营业执照 + 对公账户打款验证，支持全部功能

本小程序使用个人主体即可满足需求。

### 步骤 5：完善小程序信息

注册完成后，进入 **设置 → 基本设置**，填写：

- **小程序名称**：AI智能日历（或自定义，2-40个字）
- **小程序头像**：上传 App 图标（建议 144x144px PNG）
- **小程序介绍**：AI智能日历，用AI帮你智能规划每一天
- **服务类目**：选择 **工具 → 日历**

### 步骤 6：获取 AppID 和 AppSecret

进入 **开发 → 开发管理 → 开发设置**：

1. 复制 **AppID（小程序ID）**，格式如 `wx1234567890abcdef`
2. 点击 **重置** 获取 **AppSecret（小程序密钥）**

> 将 AppID 填入 `miniprogram/project.config.json` 的 `appid` 字段
> 将 AppID 和 AppSecret 配置到后端环境变量 `WX_APPID` 和 `WX_SECRET`

---

## 服务器域名配置

微信小程序要求所有网络请求的域名必须在白名单中。

### 配置位置

**微信公众平台后台** → **开发** → **开发管理** → **开发设置** → **服务器域名**

### 需要配置的域名

| 类型 | 说明 | 域名示例 |
|------|------|----------|
| **request 合法域名** | API 请求域名 | `https://your-app.up.railway.app` |
| **uploadFile 合法域名** | 文件上传域名（暂不使用） | 与 request 相同 |
| **downloadFile 合法域名** | 文件下载域名（暂不使用） | 与 request 相同 |

### 注意事项

1. **必须使用 HTTPS**：微信强制要求域名使用 HTTPS 协议（Railway 自动配置）
2. **不能使用 IP 地址**：必须是备案过的域名
3. **不能使用端口号**：只能使用 443 默认端口（如果后端在非标准端口，需要用 Nginx 反向代理）
4. **每项最多添加 200 个域名**
5. **一个月内最多修改 50 次**
6. **新域名生效时间**：配置后即时生效，小程序端可能出现几分钟的缓存延迟

### 开发阶段配置

如果仍在使用本地开发，需要在开发者工具中：
- 点击 **详情 → 本地设置** 
- 勾选 **"不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书"**

> 此选项仅在开发者工具中有效，真机调试和正式版不可用。

---

## 小程序代码准备

### 步骤 1：替换 AppID

编辑 `miniprogram/project.config.json`，将 `appid` 字段改为你的真实 AppID：

```json
{
  "appid": "wx1234567890abcdef",
  ...
}
```

### 步骤 2：配置生产环境 API 地址

编辑 `miniprogram/app.js`，修改 `baseUrl` 为你的后端 HTTPS 域名：

```javascript
globalData: {
  baseUrl: 'https://your-app.up.railway.app/api',
}
```

### 步骤 3：准备 TabBar 图标

`app.json` 中 tabBar 配置引用了 6 张图片，需要准备以下图标（建议尺寸 81x81px，PNG 格式）：

| 文件路径 | 用途 |
|----------|------|
| `images/calendar.png` | 日历 - 未选中 |
| `images/calendar-active.png` | 日历 - 选中（蓝色） |
| `images/ai.png` | AI建议 - 未选中 |
| `images/ai-active.png` | AI建议 - 选中 |
| `images/profile.png` | 我的 - 未选中 |
| `images/profile-active.png` | 我的 - 选中 |

> 图标设计建议：建议使用 TDesign 风格简洁图标，可通过以下方式获取：
> - TDesign 图标库：https://tdesign.tencent.com/miniprogram/components/icon
> - Iconfont：https://www.iconfont.cn/
> - 自行使用 Figma/Sketch 绘制

### 步骤 4：确认生产配置

确保 `project.config.json` 中以下选项配置正确：

```json
{
  "setting": {
    "es6": true,                // ✅ ES6→ES5 编译
    "enhance": true,            // ✅ 增强编译
    "postcss": true,            // ✅ 样式补全
    "minified": true,           // ✅ 代码压缩
    "minifyWXML": true,         // ✅ WXML 压缩
    "minifyWXSS": true,         // ✅ WXSS 压缩
    "urlCheck": true,           // ✅ 域名校验（生产必须开启）
    "uploadWithSourceMap": false, // ✅ 不上传 source map（安全）
    "lazyloadPlaceholderEnable": false
  }
}
```

### 步骤 5：代码检查清单

在上传前，请逐项检查：

- [ ] 小程序 JS 文件中没有任何 `console.log` 残留
- [ ] `app.js` 中的 `baseUrl` 指向生产环境 HTTPS 域名（非 localhost）
- [ ] `app.json` 中的所有页面路径存在且正确
- [ ] TabBar 图标文件已放置到 `images/` 目录
- [ ] `sitemap.json` 配置允许所有页面被索引
- [ ] 没有使用任何测试/调试代码

---

## 微信开发者工具上传

### 步骤 1：打开项目

1. 启动微信开发者工具
2. 选择 **小程序** 项目类型
3. 点击 **导入**，选择 `miniprogram/` 目录
4. 填写 AppID（从微信公众平台获取）

### 步骤 2：预览测试

1. 在开发者工具中点击 **预览**
2. 用微信扫描二维码，在手机上测试所有功能：
   - 登录/退出登录
   - 创建/编辑/删除日程
   - AI 建议生成
   - 日历月份切换
   - 所有 tabBar 页面跳转

### 步骤 3：上传代码

1. 确认测试通过后，点击开发者工具右上角 **上传**
2. 填写版本号和更新说明：

```
版本号：1.0.0
项目备注：首次发布 - AI智能日历小程序
```

3. 点击 **上传**，等待上传完成

### 步骤 4：设为体验版

1. 登录 [微信公众平台](https://mp.weixin.qq.com/)
2. 进入 **管理 → 版本管理**
3. 在 **开发版本** 中找到刚上传的版本
4. 点击下拉箭头 → **选为体验版**

5. 在 **成员管理** 中添加体验成员（如需要），让其他人在手机上体验测试

---

## 提交审核

### 步骤 1：进入审核页面

在 **版本管理** 页面，找到体验版，点击 **提交审核**。

### 步骤 2：填写审核信息

| 字段 | 填写内容 |
|------|----------|
| **功能页面** | 选择小程序核心功能页面（如首页、添加日程页、AI建议页） |
| **标题** | AI智能日历 |
| **所在服务类目** | 工具 → 日历 |
| **标签** | AI、日历、日程管理、智能规划 |

### 步骤 3：配置功能页面

选填至少一个核心功能页面：

```
pages/index/index — 日历首页（核心功能）
pages/add-schedule/add-schedule — 添加日程
pages/suggestion/suggestion — AI建议
pages/profile/profile — 个人中心
```

### 步骤 4：提交

确认信息无误后，点击 **提交审核**。

审核通常需要 **1-7 个工作日**，首次提交可能需要更长时间。

---

## 审核注意事项

### 1. 隐私协议

微信要求小程序必须提供隐私保护指引。虽然是工具类小程序，仍需配置：

**配置位置**：设置 → 基本设置 → 服务内容声明 → 隐私保护指引

**建议内容**：
- 说明收集的用户信息类型（微信昵称、头像、日程数据）
- 说明信息使用目的（提供日程管理服务、AI建议生成）
- 说明信息安全保护措施
- 提供用户反馈渠道

### 2. 用户授权

小程序使用了以下权限，需确保在代码中有明确的授权流程：

- **用户信息（昵称、头像）**：通过 `wx.getUserProfile` 或 `open-type="getUserInfo"` 获取（已实现）
- **登录授权**：通过 `wx.login` 获取登录凭证（已实现）

> 首页不能强制要求登录才能浏览内容。建议设计为"浏览模式"+"登录解锁全部功能"的模式。

### 3. 内容安全

由于本小程序涉及 AI 生成内容，需特别注意：

- AI 生成的建议内容需有免责声明（如"AI建议仅供参考"）
- 用户输入的日程内容不应被公开显示给其他用户
- 建议在 AI 建议展示区域添加标注：**"本建议由AI生成，仅供参考"**

### 4. 功能完整性

审核人员会测试以下功能，请确保正常：

- [ ] 登录功能正常
- [ ] 创建日程可成功保存
- [ ] 日程列表正确显示
- [ ] AI 建议能够生成（或正确显示降级原因）
- [ ] 删除/编辑日程正常
- [ ] 页面跳转无错误
- [ ] 无闪退/卡死现象

### 5. 常见审核驳回原因

| 驳回原因 | 解决方案 |
|----------|----------|
| **功能不完整** | 确保所有功能可正常使用，不要有"功能开发中" |
| **缺少隐私协议** | 在后台配置隐私保护指引 |
| **服务类目不匹配** | 确认选择"工具 → 日历" |
| **页面空白/报错** | 全路径测试所有页面和功能 |
| **缺少运营内容** | 新建时就填充一些示例数据 |
| **诱导分享** | 不要在审核版本中包含分享有奖等诱导内容 |

---

## 版本号与更新日志模板

### 版本号规范

采用 **主版本号.次版本号.修订号**（语义化版本规范）：

| 版本号 | 说明 | 示例 |
|--------|------|------|
| **1.0.0** | 首次正式发布 | 核心功能完整 |
| **1.1.0** | 新增功能 | 添加日程分享 |
| **1.1.1** | Bug修复 | 修复AI建议偶尔失败 |
| **2.0.0** | 重大更新 | 全新UI改版 |

### 更新日志模板

上传每次更新时，在备注中建议使用以下格式：

```
v1.0.0 — 首次发布（2026-06-03）

新增功能：
- ✨ 日历月视图与日程管理
- ✨ 7大场景AI智能建议生成
- ✨ 微信一键登录
- ✨ 日程编辑与删除

优化改进：
- 关键词权重系统实现零延迟分类
- setData批量优化减少线程通信
- 统一API响应格式与错误处理
```

---

## 发布后检查清单

版本审核通过后，发布到线上前请完成以下检查：

### 立即检查

- [ ] 在 **版本管理** 中 **发布** 审核通过的版本
- [ ] 用微信搜索小程序名称，确认可搜到
- [ ] 在使用微信账号正常登录测试
- [ ] 测试创建日程并生成AI建议
- [ ] 测试编辑和删除日程
- [ ] 确认所有 tabBar 页面切换正常

### 24 小时内检查

- [ ] 查看后端服务日志，确认无异常报错
- [ ] 检查数据库是否正常写入用户数据
- [ ] 查看微信公众平台 **数据分析**，确认有访问数据
- [ ] 检查 AI 建议生成成功率

### 持续监控

- [ ] 关注用户反馈和评价
- [ ] 定期查看后端错误日志
- [ ] 关注 OpenRouter API 余额（如有使用）
- [ ] 关注 Railway 资源使用量

---

## 关键链接汇总

| 名称 | 链接 |
|------|------|
| 微信公众平台 | https://mp.weixin.qq.com/ |
| 微信开发者工具下载 | https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html |
| 小程序开发文档 | https://developers.weixin.qq.com/miniprogram/dev/framework/ |
| 审核规范 | https://developers.weixin.qq.com/miniprogram/product/review.html |
| Railway 控制台 | https://railway.app/dashboard |
| OpenRouter API | https://openrouter.ai/ |
| TDesign 小程序组件 | https://tdesign.tencent.com/miniprogram/ |
